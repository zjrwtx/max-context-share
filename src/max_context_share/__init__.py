r"""max-context-share: Export/import OpenClaw context bundles.

This package provides both sync and async APIs for packaging
and unpacking OpenClaw skills, workspace files, and config
fragments as portable .tar.gz bundles.

Attributes:
    __version__ (str): The current version of the package.
"""

__version__ = "0.1.0"
